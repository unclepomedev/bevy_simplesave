[package]
name = "bevy_simplesave"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
ron = "0.12"
directories = "6.0"

[dev-dependencies]
tempfile = "3"
